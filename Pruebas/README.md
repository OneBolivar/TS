# TS
Esta sera la carpeta  general para TypeScript

# 28/07/26
1. 
 - Creamos un archivo app.ts y luego de colocar codigo usamos   el comando (dentro de la ruta en la que esta el archivo) tsc [nombre del archivo].ts

 - Ejecutamos en consola con el node [nombre del archivo].ts // tambien se puede ejecutar en el index

 - Se puede configurar un tsconfig.json, para que se actualice y se cree el .js solo colocando el tsc


2. 
- Creamos un archivo index.ts y hacemos una interfaz para clan y otra para coders (aqui guardaremos los datos), luego usamos el tsc